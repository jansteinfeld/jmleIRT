# Coefficients for jmleIRT objects

Coefficients for jmleIRT objects

## Usage

``` r
# S3 method for class 'jmleIRT'
coef(object, ...)
```

## Arguments

- object:

  An object of class `"jmleIRT"`.

- ...:

  Currently unused.

## Value

A list with components `theta` and `beta`.
